---
title: First blog
description: How to deploy your Next.js apps on Vercel.
date: '2023-10-03'
---

export const exampleCode = 
`
function getRandomNumber(min, max) {
  if (min > max) {
    // Swap min and max values if min is greater than max
    [min, max] = [max, min];
  }
  return Math.floor(Math.random() * (max - min + 1) + min);
}

// Example usage
const minNumber = 1;
const maxNumber = 100;
const random = getRandomNumber(minNumber, maxNumber);
console.log(\`Random number between \${minNumber} and \${maxNumber}: \${random}\`);
`

### test

[test](https://dev-chalvinwz.vercel.app)

<CodeHighlight code={exampleCode} language='js' />

<Image
	src='https://images.unsplash.com/photo-1696047072711-6f916b16324e?auto=format&fit=crop&q=80&w=1887&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D'

/>
